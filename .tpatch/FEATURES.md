# Tracked Features

| Slug | Title | State | Compatibility |
|------|-------|-------|---------------|
| `agent-work-status-dashboard` | Track which worktree agents are currently working, halted, or need attention (e.g., approval for a command). tws status or tws list could show per-worktree agent status: active (agent running), idle (no agent), blocked (needs approval/input). Could integrate with tpatch feature states. Useful for orchestrators to know which branches need intervention. | applied | unknown |
| `amend-aware-rebase` | When a base branch has amended commits, downstream branches hold stale SHAs that create ghost conflicts during rebase. Track last_base_sha per branch in stack.yaml. On sync, use git rebase --onto to only replay commits unique to each branch, skipping the stale base commits entirely. This avoids conflicts from amends, fixups, and interactive rebases on parent branches. Medium effort but high value for stacked PR workflows where amends from code review are common. Do NOT recommend users avoid amending — the tool should handle any git workflow gracefully. | applied | unknown |
| `archive-worktree` | Add a tws archive command that removes the worktree from disk but keeps the branch reference in stack.yaml. This saves disk space for large repos where worktrees accumulate. A tws restore or tws new with the same branch name should be able to recreate the worktree from the archived branch. Long-term quality of life improvement. | applied | unknown |
| `auto-detect-repos-workspaces` | We should try and see if we are already in a git repo to define if we should use the default workdir or a custom one for the current repo, and in case we are inside of a worktree we can work backwards and make ts work the same in every of the workspaces folders | applied | unknown |
| `auto-read-decisions-hooks` | Investigate and implement automatic decision/message reading via agent hooks. Claude Code supports hooks in settings.json that trigger on events. Copilot and other agents may support file watchers. When a new decision is written to decisions.yaml, the agent in a sibling worktree could be notified automatically. This would make the cross-worktree communication more real-time without manual tws decisions checks. | applied | unknown |
| `branch-name-decoupling` | Decouple worktree directory name from git branch name. Many repos enforce branch naming policies (e.g., user/feature/name). Currently stack.yaml name == git branch name, causing: nested worktree dirs when branch has slashes, broken symlink depths, rename destroys untracked content. Add branch_prefix config or name mapping in stack.yaml (display_name + branch_name). Keep worktree dirs flat by sanitizing slashes. Make rename preserve untracked files (move vs re-checkout). Fix doctor/list to handle nested dirs correctly. | applied | unknown |
| `checkout-agent-sessions` | Add safe direct and tmux agent sessions for checkout workspace mode. Activate the requested logical Git branch in the single physical checkout, enforce exclusive branch-owning session state, namespace sessions by stable workspace ID, inject feature context into ignored local paths, launch/resume the configured agent, and restore the original branch when a direct session exits or `tws close` ends a tmux session. Reject open --all, dirty/detached repos, active checkout sync transactions, multi-repo state, and concurrent branch sessions. Preserve external open/close behavior exactly. Update embedded skills. | applied | unknown |
| `checkout-doctor-observability` | Add checkout-mode doctor and list observability: report workspace mode/stable identity, current Git branch and matching logical feature/entry, dirty/detached/active Git operation state, checkout sync transaction/lock and recovery status, checkout agent/tmux session ownership and stale state, branch existence/archive status, parent-child ancestry, and injected context link health. Distinguish materialized external worktrees from checkout logical branches. Preserve external doctor/list behavior. Update embedded skills. | applied | unknown |
| `checkout-existing-branch` | Allow tws new to check out an existing branch into a feature worktree instead of only creating new branches. Should work even if the branch is currently checked out in the main repo (git worktree add --detach the main repo first, or use a different approach). This enables migrating existing in-progress work into the tesseraworkspaces model. | applied | unknown |
| `checkout-stack-safety` | Implement safe stack synchronization for checkout workspace mode using the single physical Git checkout. Enforce a clean attached checkout, acquire an operation lock, persist a transaction with original branch/HEAD and ordered plan, sequentially switch and rebase logical branches, restore the original branch on success and abort, and support interruption/conflict recovery through sync --continue/--abort. Reuse explicit-base semantics, amend-aware rebasing, validation commands, and final ancestry checks. Preserve external sync behavior exactly. Update embedded skills when behavior stabilizes. | applied | unknown |
| `checkout-workspace-lifecycle` | Add the explicit checkout-workspace lifecycle on top of workspace-mode-foundation. Enable a repository-local single-checkout mode with metadata under <repo>/.tws/features and runtime state under <repo>/.tws/state, ignored locally through .git/info/exclude. Add init/enable/mode commands; make add/new/rename/archive/delete/export/import mode-aware; checkout new creates logical stack entries and Git branches but no linked worktrees. Preserve external mode behavior exactly. Branch deletion remains explicit and destructive only. Update embedded skills after behavior stabilizes. | applied | unknown |
| `clean-git-output` | Filter and format git output during tws sync. Suppress git hint: messages, wrap rebase warnings in cleaner format. The cherry-pick skip warnings (warning: skipped previously applied commit) are confusing — could suppress or reformat them. Low priority cosmetic improvement. | applied | unknown |
| `cobra-migration` | Migrate the CLI from manual os.Args parsing to cobra for cleaner flag handling, auto-generated help, and built-in shell completion (bash, zsh, fish, powershell). All 10 commands (add, new, open, sync, stack, delete, list, archive, init) plus global flags (--version, --help) need migration. | applied | unknown |
| `config-edit-command` | Add tws config command to edit configuration from the CLI. Subcommands like tws config set agent_command opencode, tws config set use_tmux true, tws config show. Should support --repo flag to edit per-repo config vs global. Avoids users needing to remember YAML syntax. | applied | unknown |
| `context-summarizer` | Multi-level context summarization for features and sessions. Auto-populate FEATURE.md with evolving summaries of what the feature does (not just the name). Session-level recap: before resuming a worktree, show a brief summary of what was done in the last session (broader than copilot-cli's single-turn recap). Could use an LLM to summarize git log, decisions, and file changes. Three levels: feature overview (FEATURE.md), session summary (per-worktree), and turn recap (delegate to agent). | requested | unknown |
| `copilot-codex-hooks` | Add hooks support for GitHub Copilot CLI and Codex CLI alongside the existing Claude Code hooks. Copilot uses .github/copilot-instructions.md or AGENTS.md for persistent instructions. Codex may support similar mechanisms. Investigate each agent's hook/instruction system and add tws hooks install --agent copilot/codex. Update the embedded skills to match. | requested | unknown |
| `cross-worktree-agent-context` | The CLAUDE.local.md at the feature level is a start for shared context across worktrees, but we need a mechanism for agents to advertise design decisions and breaking changes to sibling branches. Add a changes.log or decisions.yaml at the feature level where agents write design decisions that affect sibling branches. Agents in child branches can read these and decide if they need to adapt. This is the cross-worktree semantic communication layer that no existing stacked-diff tool provides. | applied | unknown |
| `decision-read-tracking` | Track which decisions each worktree has already seen. Store a last-read decision ID per branch (in the feature dir or worktree). tws decisions --mine only shows unread decisions. tws decisions ack marks all as read. tws open shows count of new (unread) decisions instead of total. Prevents agents and users from re-reading the same decisions every session. | applied | unknown |
| `delete-feature` | Add a tws delete (or tws rm) command that removes a feature and all its worktrees. Should remove the worktrees via git worktree remove, delete the feature directory from the workspace, and clean up stack.yaml. Branches are left intact in git for now. | applied | unknown |
| `divergent-stack-sync` | The stack.yaml data model should support DAG dependencies from day one (A->B, A->C divergent stacks), but only implement linear sync initially. Divergent sync (rebasing B and C independently from A) is a future feature. The data model cost of supporting DAGs upfront is minimal, and retrofitting it later into a linear-only schema would be painful. | applied | unknown |
| `docs-v1-2-release-candidate` | Update CHANGELOG, README, cheatsheet, configuration guide, and add a v1.2 RC validation guide for configured-base checkout, feature-directory repo inference, corrected sync continuation, decoupled branch identity, and feature-level open. Link the committed roadmap. Documentation only. | applied | unknown |
| `fix-add-tmux-flag` | Add --tmux flag to tws add --open so quick start can open in a tmux session instead of only direct mode. Previously tws add -n branch --open always used openDirect. | applied | unknown |
| `fix-atomic-rename` | tws rename branch is non-atomic: it writes stack.yaml (metadata rename) BEFORE validating the git repo is reachable. If run from the feature dir (not a git repo), the git step fails but stack.yaml is already corrupted with the new name while git branch + worktree dir retain the old name. Fix: validate git repo reachability before mutating any metadata. Roll back stack.yaml on git failure. Also resolve the source repo from feature metadata (stack.yaml Repo field or active worktree) so commands work from the feature dir. | applied | unknown |
| `fix-auto-select-behavior` | When tws open auto-selects a single feature and single branch, it should still print what was selected so the user knows what happened. Verify it's actually opening the worktree and not the current dir. Add a confirmation or at least a clear message before launching the agent. | applied | unknown |
| `fix-checkout-feature-path-routing` | Complete the checkout layout repair: Workspace.FeaturePath must return `.tws/features/<feature>` for checkout mode and external paths unchanged; existing checkout lifecycle commands that operate on existing features must use error-returning ResolveFeaturePath to discover legacy `.tws/<feature>` or new layout and reject ambiguity. New add/import writes only new layout. Checkout sync/session state remains `.tws/state`. Add full lifecycle legacy/new regression tests and update skills. | applied | unknown |
| `fix-checkout-lifecycle-layout` | Repair the released checkout workspace lifecycle before agent sessions. Wire the documented `tws enable --mode ...` and `tws mode` commands, store new checkout feature metadata under `<repo>/.tws/features/<feature>`, make feature listing mode-aware, and avoid treating `.tws/state`, templates, config, or other internal directories as features. Preserve backward compatibility for v1.2.2/v1.2.3 users with legacy `<repo>/.tws/<feature>` metadata through explicit legacy discovery/migration behavior. Keep external workspace paths unchanged. Update embedded skills and lifecycle tests. | applied | unknown |
| `fix-ci-rebase-editor` | Make checkout sync conflict-continuation integration tests deterministic in noninteractive CI by setting a no-op Git editor for test Git commands. GitHub Actions currently fails `git rebase --continue` on Linux and macOS with `Terminal is dumb, but EDITOR unset`, while local shells pass. Product behavior is unaffected. Add regression coverage and restore green main/tag CI before continuing checkout-agent-sessions. | applied | unknown |
| `fix-default-base-branch` | Default --base is hardcoded to "main" but many repos use "master" or other default branches. Detect the actual default branch from origin/HEAD (git rev-parse --abbrev-ref origin/HEAD) and use that as the default. Warn if the given base branch doesn't exist. | applied | unknown |
| `fix-external-feature-dir-resolution` | Restore the external workspace contract after workspace-mode refactoring: feature-oriented commands must resolve an external workspace and its default source repository when invoked from the external workspace root, a feature directory, or nested docs/inject paths, not only from Git repos/worktrees. Use the .tws-workspace marker, sibling repo convention, stack/worktree metadata fallback, and explicit ambiguity errors. Preserve checkout-mode resolution and multi-repo behavior. Add a command-location regression matrix for stack, sync, decisions, doctor, list, inject, and other feature commands. | applied | unknown |
| `fix-initial-claude-session` | When opening a worktree for the first time via tws open, the tmux command runs "claude -c" which fails because there is no existing session to continue. On first open we should run "claude" (or the configured agent command) without the -c flag. Detect whether a session already exists to decide which variant to use. | applied | unknown |
| `fix-missing-completions` | Add ValidArgsFunction to new, sync, stack, and delete commands that were missing tab completions after the cobra migration. All commands that take feature/branch args now provide shell completion suggestions. | applied | unknown |
| `fix-new-base-checkout` | Ensure tws new and tws add -n create new Git branches from the configured base ref in the selected repository. Explicit refs are literal; omitted bases use that repository's origin/HEAD. Infer a single source repo when invoked from a feature directory, fail on ambiguity, and validate before writing workspace metadata. Update embedded skills when agent-facing behavior changes. | applied | unknown |
| `fix-open-cwd-after-exit` | When using tws open without tmux, exiting the agent leaves the user at the repo root instead of the worktree dir. syscall.Exec replaces the process so the parent shell cwd is unchanged. Fix by using exec.Command with Dir set, or consider a shell wrapper approach like worktrunk's shell install. The user should end up in the worktree directory after the agent exits. | applied | unknown |
| `fix-space-test-git-maintenance-race` | Make the spaces absent-registry no-side-effect test ignore only transient Git lock files under .git (such as objects/maintenance.lock), while still comparing every other path — including the tws-owned .git/tws/workspace-id and .git/info/exclude — and explicitly asserting that spaces.yaml, .spaces.lock, and workspace markers are not created. | applied | unknown |
| `fix-status-tmux-test-portability` | Make agent status CLI tests deterministic across Linux/macOS runners by injecting explicit tmux-no-server and tmux-missing fixtures instead of depending on host tmux installation. | applied | unknown |
| `fix-sync-branch-identity` | Use StackEntry.GitBranch() rather than the short tws name for active sync branch validation and Git ref operations, while preserving the short name in user-facing output. Update embedded skills when agent-facing behavior changes. | applied | unknown |
| `fix-sync-continue-descendants` | Fix tws sync --continue so descendants deferred by a parent conflict return to pending, later conflicts preserve state, and Sync complete is printed only after all relevant parent-child ancestry edges are current. Update embedded skills when agent-facing behavior changes. | applied | unknown |
| `gitignored-scratch-context` | First-class support for untracked per-worktree context that never shows in git status. A documented scratch/context inject mode that targets a known gitignored path (configurable, default .tws/ or dev/). Auto-verify the target is gitignored. Currently users rely on manual .gitignore + hand-symlinks. | requested | unknown |
| `go-module-version-fallback` | Make tws --version report the tagged module version when installed with go install github.com/jdbencardinop/tesseraworkspaces/cmd/tws@latest, while preserving ldflag-injected source builds and adding workflow coverage so dev cannot silently ship. | applied | unknown |
| `inject-custom-target-path` | Add --into flag to tws inject for specifying a custom target subdirectory inside the worktree. Currently inject/ maps to the worktree root. With --into, users can target a gitignored subfolder like .context/ or .claude/. Example: tws inject auth --into .context would symlink inject/ contents into <worktree>/.context/ instead of <worktree>/. Users can work around this today by nesting folders inside inject/ (e.g., inject/.context/file.md) but this should be documented better and the --into flag adds clarity. | applied | unknown |
| `inter-feature-messaging` | Allow orchestrators in separate features under the same workspace to exchange durable targeted messages without merging feature workspaces. Extend the decisions model with a feature target (`--to-feature`), workspace-level storage/routing, per-feature read tracking, and clear source/target identities. Keep each feature's stack, inject files, and lifecycle independent. Update embedded skills when agent-facing behavior changes. | requested | unknown |
| `interactive-open-picker` | When tws open is called without arguments, show an interactive picker (via fzf or built-in) to select a feature and branch. Could also support tws open <feature> with no branch to pick from branches within that feature. Shell completions from cobra should also suggest valid features and branches. | applied | unknown |
| `keep-track-of-stacked-diffs-and-dependencies` | Our sync method currently just rebases every single branch we have a worktree for, but this might not be what we want, we might just need to advance some of the worktrees or take just changes in one worktree, check if we need one worktree per stack or one worktree per feature, we need to implement a better approach to handling a graph of dependencies in the changes | applied | unknown |
| `lightweight-worktree-handler` | We should have a way to work with worktrees that suits our way of modeling our features, in which a feature folder can have many worktrees, so not all worktrees in the repo are in the same folder, worktrunk might do that out of the box, so we should also document how to do that in worktrunk | applied | unknown |
| `list-features-branches` | Add a tws list (or tws ls) command that shows all features and their branches in the current workspace. Could integrate with fzf for interactive selection or shell completions in a future feature. | applied | unknown |
| `multi-repo-workspaces` | Formalize adding worktrees from different repos in the same feature workspace. Currently possible by manually setting paths, but not tracked properly in stack.yaml or tws list/status. Should support: tws new <feature> <branch> --repo /path/to/other/repo, track the source repo per branch in stack.yaml, pass decisions across repo boundaries within the same feature. | applied | unknown |
| `named-feature-templates` | Add a managed named-template registry for feature creation. `tws template add <name> <path>` copies a template into global or per-repo tws storage; list/show/remove provide discovery and lifecycle management; `tws add <feature> --template <name>` resolves a stable template. Templates have explicit `feature/` assets copied into the feature root and `inject/` assets copied into the worktree injection source. Support feature-space orchestrator skills, docs, prompts, and shared inject files. Update embedded skills when agent-facing behavior changes. | requested | unknown |
| `open-feature-dir` | Allow tws open <feature> (no branch) to open the feature directory itself, not just worktrees. Useful for viewing docs, inject files, decisions. Optionally create a tmux session for the whole feature with --all flag that creates windows for each worktree branch in the same session. This enables a single tmux session to manage the entire feature with tabs/windows per branch. | applied | unknown |
| `open-worktree-command` | Add explicit non-agent launch surfaces to tws open without changing the configured agent_command: a machine-readable path-only mode suitable for shell integration, and a per-invocation command/editor mode for tools such as nvim in the selected worktree or feature directory, supporting direct and tmux execution where meaningful. Preserve the existing --no-agent human guidance and normal agent-session behavior; do not claim a child process can change the parent shell cwd; pass command arguments without shell eval; do not misreport a generic editor as semantic agent work; and keep lifecycle guards, injection, workspace-mode routing, and existing open output compatible. | requested | unknown |
| `per-file-inject-routing` | Support per-file inject targets via an inject.yaml mapping. Currently inject_into is a single global value. Users need CLAUDE.local.md at worktree root (agent auto-discovery) but planning docs in a gitignored subfolder (dev/). inject.yaml would map files to destinations: CLAUDE.local.md → ., *.html → dev/, planning/ → dev/planning/. Directory-mirroring mode: inject/dev/foo lands at worktree/dev/foo naturally. This unblocks F1, F2, F4 from user feedback. | applied | unknown |
| `per-repo-config` | Add support for a .tws/config.yaml in the repo root for per-repo configuration. This would override global config for that repo. Settings like agent_command, use_tmux, and workspace path could be set per-repo and committed. Deferred from user-defined-workdir. | applied | unknown |
| `persist-agent-workflow-guidance` | Add repository-root AGENTS.md and CLAUDE.md pointers plus a durable engineering workflow document so Claude Code, Copilot CLI, and future agents can recover the roadmap, current mode program, tpatch Path B/land process, review loop, tests, release cadence, and coding safety conventions after context compaction or a new session. | applied | unknown |
| `pluggable-agent-command` | ts open currently hardcodes "claude -c" as the agent command launched in the tmux session. We should make this configurable so users can use other agents (opencode, aider, cursor, etc) or no agent at all. Add an agent_command field to the global config (~/.config/ts/config.yaml) and support a per-feature override. Also add claude to the README requirements for now, and document how to change the agent command. | applied | unknown |
| `post-rebase-validation` | Add a configurable validation command (test_command in config) that runs after each successful rebase during tws sync. Example: test_command: go build ./... or test_command: npm run typecheck. Catches syntax errors and type errors before moving to the next branch. If validation fails, treat like a rebase failure — skip descendants. Support per-repo config. Update skills to mention validation. | applied | unknown |
| `push-branches` | Two push modes: (1) tws sync <feature> --push pushes all successfully synced branches with --force-with-lease after sync completes. (2) tws push <feature> as a standalone command that pushes all branches in the stack with --force-with-lease. Should only push branches that have diverged from their remote tracking branch. Support --dry-run to preview what would be pushed. Update skills to document both push workflows. | applied | unknown |
| `quick-start-add-and-open` | Add a shortcut to tws add that creates a feature, creates a first worktree branch, and optionally opens it in one command. Something like tws add <feature> -n <branch> [--base <parent>] [--open]. Saves running tws add + tws new + tws open separately for the common case of starting fresh work. | applied | unknown |
| `quiet-fetch-output` | Make git fetch quiet by default during tws sync. Use git fetch --quiet to suppress the branch/tag listing that can be hundreds of lines. Add a verbose flag (tws sync --verbose or -v) to show full fetch output when needed. Add fetch_quiet config option (default: true). Update skills to mention this. | applied | unknown |
| `readme-v1-update` | Update README.md for the v1.0.0 release. Document all commands (22 total), the 3-tier skill system, decisions/hooks workflow, workspace portability (export/import), multi-repo workspaces, sync --continue/--push, post-rebase validation, inject system with --into, templates, and known caveats. Add badges, update install instructions, and link to docs/. | applied | unknown |
| `rebrand-to-tesseraworkspaces` | Rename the project from tesseraspaces/ts to tesseraworkspaces/tws to avoid collision with the TypeScript compiler (ts). Update module path, binary name, all imports, Makefile, README, docs, and CI workflow. | applied | unknown |
| `refresh-roadmap-after-registry` | Refresh the durable roadmap and engineering workflow after checkout doctor v1.2.9 and workspace registry landed: mark both capabilities shipped and set workspace-sibling-links as the current target without changing product behavior. | applied | unknown |
| `rename-feature-worktree` | Add tws rename command to rename a feature or a worktree/branch within a feature. For features: rename the directory in the workspace. For branches: update stack.yaml entry, rename the worktree directory, and optionally rename the git branch. Need to handle references from other stack entries (base pointers) and tmux sessions. | applied | unknown |
| `repair-historical-tpatch-artifacts` | After upgrading to the latest tpatch version, methodically audit and repair historical feature artifacts whose generated recipes replay but canonical patches fail dependency-closure verification because older bundled shared-file baselines are incomplete. Work topologically, preserve append-only generations, add truthful dependencies or narrow feature boundaries, use tpatch rebind/repair tooling if available, and produce a verification report. This is deferred backlog maintenance and must not block product work while recipe replay and code tests remain green. | requested | unknown |
| `skill-distribution` | Add a tws init command that installs agent skills teaching coding agents how to use tws. Supports Claude Code (.claude/skills/) and GitHub Copilot (.github/prompts/ and copilot-instructions.md). Skills are embedded in the Go binary via go:embed. Project-local install by default. | applied | unknown |
| `stack-ancestry-doctor` | Extract one mode-independent, read-only stack edge evaluator and integrate it into tws doctor. For every configured parent-child edge, use StackEntry.GitBranch(), literal base refs, the current parent head, and LastBaseSHA to classify current, stale, divergent, missing, or cross-repo; report the relevant heads/merge base/last base plus actionable guidance. Support external and checkout modes without fetching or mutating Git, preserve doctor exit semantics, and expose a reusable projection for stack status and later additive tws status enrichment. | applied | unknown |
| `stack-status` | Add tws stack status <feature> [--json] while preserving the existing tws stack <feature> dependency tree. Use the shared stack ancestry evaluator to show each logical and Git branch, local head, configured parent/base and parent head, LastBaseSHA, ancestry state, materialization, dirty/rebase state, upstream, and ahead/behind counts. Keep it deterministic, read-only, local-only by default with no implicit fetch, mode-aware for external worktrees and checkout logical branches, and explicit about literal refs and cross-repo entries. | applied | unknown |
| `sync-continue` | Add tws sync <feature> --continue to resume after conflict resolution. Persist sync state (failed branch, pending branches) in a .sync-state.yaml in the feature dir. After the user resolves conflicts and runs git rebase --continue in the worktree, tws sync --continue picks up where it left off and processes remaining branches. Print clear guidance on conflict: "Resolve in <worktree>, then run tws sync <feature> --continue". Update skills to document this workflow. | applied | unknown |
| `sync-modes` | Add explicit sync modes to tws sync while preserving no-flag behavior. Distinguish local-only propagation (update selected descendants from local parent tips without advancing roots from remote bases) from no-fetch operation (use existing local and remote-tracking refs without network access). Support selecting one logical branch, an explicit root, and an optional descendant subtree; carry the selected mode, scope, push, validation, and recovery semantics through --continue and --abort; make external and checkout behavior consistent; reject incompatible combinations before mutation; and use real repositories, remotes, and worktrees for safety tests. Keep rebase-plan preview, safe reparent/restack, patch identity, and implicit multi-parent behavior out of scope. | applied | unknown |
| `targeted-worktree-messaging` | Add targeted messaging between worktrees — one worktree can send a decision or question to a specific other worktree. Builds on top of cross-worktree-agent-context broadcast pattern. Enables review/implement cycles where Agent A sends feedback to Agent B. Need to design: read tracking, threading, and how agents discover messages addressed to them. | applied | unknown |
| `template-conflict-resolution` | When applying templates and files conflict, prompt the user in apt/dpkg style: keep existing, use new, show diff, or edit merged. Interactive conflict resolution for tws add --template and tws template sync when inject/ files differ from template files. | requested | unknown |
| `template-sync-existing` | Add tws template sync command to backfill template files into existing features. Also add --template flag to tws add for specifying a template directory from anywhere on disk (not just ~/.config/tws/templates or .tws/templates). Support applying multiple templates to the same feature. When files conflict, prompt the user in an apt/dpkg style: keep existing, use new, edit to merge, or show diff. | applied | unknown |
| `tiered-skill-system` | Investigate and design a 3-tier skill system: (1) worktree-level skills — injected into each worktree, agents work on code within a branch. (2) feature-level skills — in the feature folder, an orchestrator agent can manage other agents, coordinate decisions, track progress across branches. (3) global tws skills — installed via tws init, knows how to create features and workspaces from anywhere. The feature-level orchestrator is the novel part — it could run tws commands to sync, check decisions, and direct agents in worktrees. Needs investigation into whether this orchestration pattern is practical with current agent capabilities. | applied | unknown |
| `tmux-free-mode` | Make tmux optional for tws open. Add a simpler mode that just cd's into the worktree directory and runs the agent command (claude or configured alternative) directly without tmux wrapping. tmux is nice for session persistence but not always needed. Could be a --no-tmux flag or a config option. | applied | unknown |
| `tmux-session-management` | Add tws close command to kill tmux sessions for a worktree. Detect if a worktree has an associated tmux session (via session naming convention). Prevent conflicts between tmux mode and direct mode when a stale tmux session exists. Show tmux session status in tws list. | applied | unknown |
| `tpatch-integration` | Integrate tws with tpatch so that feature workspaces can contain tpatch metadata. A meta-repo or .tws subfolder could hold tpatch's .tpatch/ structure, allowing features to be tracked as tpatch features with analysis/spec/exploration/record workflow. This bridges the workspace orchestration (tws) with the feature lifecycle (tpatch). | requested | unknown |
| `tpatch-patch-identity-research` | Research a stable, read-only tpatch patch-identity and change-equivalence contract for tws. Determine whether tpatch can identify the same logical change when commit SHAs change through rebase/amend or files are renamed/moved, using canonical patch/change IDs, generation or successor metadata, and Mercurial evolution/obsolescence markers as comparative prior art. After stack-ancestry-doctor and stack-status ship, prototype how tws could use that contract for safer rebase planning and to distinguish stale SHA identity from equivalent content. Do not implement patch theory, reconciliation, or commit-successor ownership inside tws; document available tpatch commands, gaps, versioned JSON requirements, false-positive/false-negative risks, and an integration recommendation. | requested | unknown |
| `update-embedded-skills` | Update the embedded Claude Code and Copilot skill files (assets/skills/) to cover all features added since the initial skill creation. New features to document in skills: decisions (decide, decisions show/ack), inject system, templates, config command, doctor, rename, archive, quick start (tws add -n --open), shell completions. Also document the inject/ directory structure and how agents should use CLAUDE.local.md for context. | applied | unknown |
| `update-github-actions-node24` | Update GitHub Actions dependencies to versions that natively support Node.js 24. Current CI is green, but checkout@v4 and setup-go@v5 emit deprecation warnings because GitHub forces their Node 20 runtime to Node 24. Verify replacement action versions, preserve Go caching/version-file behavior, and keep the Linux/macOS/tag/release matrix green. | requested | unknown |
| `update-skills-p0-correctness` | Update embedded Claude and Copilot skills for P0 configured-base semantics, feature-directory repo inference, actual Git branch identity, and sync --continue descendant completion. Depends on all three P0 fixes. | applied | unknown |
| `update-skills-v0-8-1` | Update embedded Claude and Copilot skill files to cover v0.8.1 features: tws push, tws sync --push/--verbose, test_command config for post-rebase validation. | applied | unknown |
| `user-defined-workdir` | Currently we use a default working dir for everythin under /Users/jbencardino/ts, but we should have a workspace per repo and let the user configure where to set it up | applied | unknown |
| `versioned-builds` | Add --version and --help flags to the tws CLI. Tag builds with the git tag version via ldflags. CI should only trigger binary builds/releases on tag push; tests, lint, and formatting should run on every push to main and PRs. | applied | unknown |
| `workspace-hub-research` | Research an optional higher-level tws hub that discovers multiple project `.tws` roots and tool-owned sibling spaces (learning, tickets, patching) without becoming their authority. Evaluate whether a registry/index provides enough value before designing a super-workspace hierarchy. Coordinate contracts with tesserapatch, tesseratickets, and learning tools such as /teach. | requested | unknown |
| `workspace-mode-foundation` | Introduce an explicit persisted workspace mode enum with `external` (current sibling multi-worktree backend, default) and `checkout` (future in-repo single-checkout backend). Add a resolved Workspace value carrying stable identity, mode, repository root, metadata root, and capability flags. Route current path resolution through the external backend without changing any existing outputs or behavior. Add extensive characterization and regression tests before checkout lifecycle behavior is implemented. Update embedded skills when agent-facing behavior changes. | applied | unknown |
| `workspace-portability` | Add a way to sync or recreate workspace metadata across machines. When you push branches from one machine and pull on another, the workspace structure (stack.yaml, decisions, inject config) doesn't travel. Options: export/import commands, git-backed metadata in .tws/ inside the repo, or a tws clone command that recreates a workspace from existing branches. The goal is that when you git clone + fetch on a new machine, you can reconstruct the full tws workspace. | applied | unknown |
| `workspace-registry` | Add an opt-in global registry of active tws repositories/workspaces at `${XDG_DATA_HOME:-~/.local/share}/tws/registry.yaml`. Store schema version, stable opaque workspace ID, aliases, canonical path, kind (repo, external-workspace, checkout-workspace), and Git/marker identity hints. Provide `tws registry add/list/show/alias/check/repair/remove/prune --missing`, atomic locked 0600 writes in a 0700 directory, stale/moved/mismatched classification, and explicit enrollment only (`tws init --register` or registry add). The registry is discovery-only: removing an entry never deletes targets and it never stores messages/tool-owned content. Preserve existing workspace behavior. | applied | unknown |
| `workspace-sibling-links` | Add a workspace-level registry for tool-owned sibling spaces such as learning, tickets, patching, research, and documentation. Store dynamic links in <workspace-root>/spaces.yaml rather than embedding paths in skills. Each entry has a stable name, kind, path, optional description, and optional feature scope. Add `tws space add/list/show/remove` and no-arg auto-detection from repos, feature dirs, and worktrees. Skills should teach agents to discover links with `tws space list`; tws must not own the linked tool's schema or lifecycle. Support absolute paths and workspace-relative paths, validate existence without requiring all targets to be Git repos, and keep a higher-level multi-project hub out of scope. | applied | unknown |
| `workspace-templates` | Add configurable templates for new features. When tws add creates a feature, use a template directory (~/.config/tws/templates/ or .tws/templates/) to populate the inject/ folder with default files like CLAUDE.local.md, skills, etc. Users can customize what every new feature gets out of the box. | applied | unknown |
| `worktree-context-injection` | Add a system to inject files from the feature workspace into each worktree's working directory. When tws new or tws open runs, copy or symlink configured files (e.g., CLAUDE.local.md, .claude/skills/, decisions.yaml) into the worktree so agents auto-discover them. Support a template system in the feature dir (e.g., a .tws-inject/ folder) where users place files that should appear in every worktree. Handle both symlinks (live updates from workspace) and copies (snapshot). Consider .gitignore implications — injected files inside the repo should probably be gitignored, but that's the user's choice. This bridges the gap between our workspace (outside repo) and the agent's working directory (inside the worktree). | applied | unknown |
| `worktree-health-check` | Add branch consistency validation to tws sync and tws list. Before rebasing, verify each worktree is on the branch that stack.yaml expects. If a worktree is on the wrong branch (user switched manually), warn and skip it. tws list could show a warning indicator. Could also add a tws doctor command for full workspace health checks. | applied | unknown |
