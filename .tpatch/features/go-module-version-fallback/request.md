# Feature Request: Make tws --version report the tagged module version when installed with go install github.com/jdbencardinop/tesseraworkspaces/cmd/tws@latest, while preserving ldflag-injected source builds and adding workflow coverage so dev cannot silently ship.

**Slug**: `go-module-version-fallback`
**Created**: 2026-05-04T20:00:04Z

## Description

Make tws --version report the tagged module version when installed with go install github.com/jdbencardinop/tesseraworkspaces/cmd/tws@latest, while preserving ldflag-injected source builds and adding workflow coverage so dev cannot silently ship.
