# Exploration

- `docs/roadmap.md`: the `Now` section still calls checkout doctor the next
  feature; its agent-integration backlog already defines workspace sibling
  links.
- `docs/engineering-workflow.md`: the shipped checkout list stops at agent
  sessions and repeats checkout doctor as next.
- `.tpatch/features/checkout-doctor-observability/status.json`: records the
  doctor feature as applied.
- `.tpatch/features/workspace-registry/status.json`: records the registry
  feature as applied.
- `.tpatch/features/workspace-sibling-links/status.json`: records the new target
  as requested with soft dependencies on registry and hub research.

The smallest change is limited to the two durable guidance documents.
