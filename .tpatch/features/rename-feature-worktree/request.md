# Feature Request: Add tws rename command to rename a feature or a worktree/branch within a feature. For features: rename the directory in the workspace. For branches: update stack.yaml entry, rename the worktree directory, and optionally rename the git branch. Need to handle references from other stack entries (base pointers) and tmux sessions.

**Slug**: `rename-feature-worktree`
**Created**: 2026-05-12T01:07:14Z

## Description

Add tws rename command to rename a feature or a worktree/branch within a feature. For features: rename the directory in the workspace. For branches: update stack.yaml entry, rename the worktree directory, and optionally rename the git branch. Need to handle references from other stack entries (base pointers) and tmux sessions.
