# Specification

1. checkout sync tests run in CI without interactive editor configuration.
2. Conflict-and-continue and second-conflict tests still exercise real `git rebase --continue`.
3. Production code is unchanged.
4. Full Linux/macOS GitHub Actions test matrix passes.
