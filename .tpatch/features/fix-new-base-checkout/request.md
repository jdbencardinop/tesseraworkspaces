# Feature Request: Ensure tws new and tws add -n create new Git branches from the configured base ref in the selected repository. Explicit refs are literal; omitted bases use that repository's origin/HEAD. Infer a single source repo when invoked from a feature directory, fail on ambiguity, and validate before writing workspace metadata. Update embedded skills when agent-facing behavior changes.

**Slug**: `fix-new-base-checkout`
**Created**: 2026-07-18T08:16:09Z

## Description

Ensure tws new and tws add -n create new Git branches from the configured base ref in the selected repository. Explicit refs are literal; omitted bases use that repository's origin/HEAD. Infer a single source repo when invoked from a feature directory, fail on ambiguity, and validate before writing workspace metadata. Update embedded skills when agent-facing behavior changes.
