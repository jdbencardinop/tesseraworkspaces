# Feature Request: Track which worktree agents are currently working, halted, or need attention (e.g., approval for a command). tws status or tws list could show per-worktree agent status: active (agent running), idle (no agent), blocked (needs approval/input). Could integrate with tpatch feature states. Useful for orchestrators to know which branches need intervention.

**Slug**: `agent-work-status-dashboard`
**Created**: 2026-07-01T04:52:47Z

## Description

Track which worktree agents are currently working, halted, or need attention (e.g., approval for a command). tws status or tws list could show per-worktree agent status: active (agent running), idle (no agent), blocked (needs approval/input). Could integrate with tpatch feature states. Useful for orchestrators to know which branches need intervention.
