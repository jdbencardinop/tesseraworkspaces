# Exploration

- `internal/workspace.go`, `paths.go`, `config.go`
- `internal/cli/root.go`, `init.go`, add/new/list/completion callers
- checkout lifecycle integration tests
- embedded Claude/Copilot skills

Tests: fresh checkout layout, legacy layout discovery/migration, collision, command wiring/help, internal-dir filtering, external regression.
