# Feature Request: Add --tmux flag to tws add --open so quick start can open in a tmux session instead of only direct mode. Previously tws add -n branch --open always used openDirect.

**Slug**: `fix-add-tmux-flag`
**Created**: 2026-05-19T23:17:43Z

## Description

Add --tmux flag to tws add --open so quick start can open in a tmux session instead of only direct mode. Previously tws add -n branch --open always used openDirect.
