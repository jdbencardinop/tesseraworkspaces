# Feature Request: Make tmux optional for tws open. Add a simpler mode that just cd's into the worktree directory and runs the agent command (claude or configured alternative) directly without tmux wrapping. tmux is nice for session persistence but not always needed. Could be a --no-tmux flag or a config option.

**Slug**: `tmux-free-mode`
**Created**: 2026-05-02T04:36:40Z

## Description

Make tmux optional for tws open. Add a simpler mode that just cd's into the worktree directory and runs the agent command (claude or configured alternative) directly without tmux wrapping. tmux is nice for session persistence but not always needed. Could be a --no-tmux flag or a config option.
