# Feature Request: When tws open auto-selects a single feature and single branch, it should still print what was selected so the user knows what happened. Verify it's actually opening the worktree and not the current dir. Add a confirmation or at least a clear message before launching the agent.

**Slug**: `fix-auto-select-behavior`
**Created**: 2026-05-05T06:49:17Z

## Description

When tws open auto-selects a single feature and single branch, it should still print what was selected so the user knows what happened. Verify it's actually opening the worktree and not the current dir. Add a confirmation or at least a clear message before launching the agent.
