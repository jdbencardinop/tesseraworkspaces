# Feature Request: Add --version and --help flags to the tws CLI. Tag builds with the git tag version via ldflags. CI should only trigger binary builds/releases on tag push; tests, lint, and formatting should run on every push to main and PRs.

**Slug**: `versioned-builds`
**Created**: 2026-05-02T04:36:11Z

## Description

Add --version and --help flags to the tws CLI. Tag builds with the git tag version via ldflags. CI should only trigger binary builds/releases on tag push; tests, lint, and formatting should run on every push to main and PRs.
