# Feature Request: Add the explicit checkout-workspace lifecycle on top of workspace-mode-foundation. Enable a repository-local single-checkout mode with metadata under <repo>/.tws/features and runtime state under <repo>/.tws/state, ignored locally through .git/info/exclude. Add init/enable/mode commands; make add/new/rename/archive/delete/export/import mode-aware; checkout new creates logical stack entries and Git branches but no linked worktrees. Preserve external mode behavior exactly. Branch deletion remains explicit and destructive only. Update embedded skills after behavior stabilizes.

**Slug**: `checkout-workspace-lifecycle`
**Created**: 2026-08-02T17:02:08Z

## Description

Add the explicit checkout-workspace lifecycle on top of workspace-mode-foundation. Enable a repository-local single-checkout mode with metadata under <repo>/.tws/features and runtime state under <repo>/.tws/state, ignored locally through .git/info/exclude. Add init/enable/mode commands; make add/new/rename/archive/delete/export/import mode-aware; checkout new creates logical stack entries and Git branches but no linked worktrees. Preserve external mode behavior exactly. Branch deletion remains explicit and destructive only. Update embedded skills after behavior stabilizes.
