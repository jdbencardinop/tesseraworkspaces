# Feature Request: Use StackEntry.GitBranch() rather than the short tws name for active sync branch validation and Git ref operations, while preserving the short name in user-facing output. Update embedded skills when agent-facing behavior changes.

**Slug**: `fix-sync-branch-identity`
**Created**: 2026-07-18T08:16:09Z

## Description

Use StackEntry.GitBranch() rather than the short tws name for active sync branch validation and Git ref operations, while preserving the short name in user-facing output. Update embedded skills when agent-facing behavior changes.
