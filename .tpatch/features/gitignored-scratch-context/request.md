# Feature Request: First-class support for untracked per-worktree context that never shows in git status. A documented scratch/context inject mode that targets a known gitignored path (configurable, default .tws/ or dev/). Auto-verify the target is gitignored. Currently users rely on manual .gitignore + hand-symlinks.

**Slug**: `gitignored-scratch-context`
**Created**: 2026-07-01T04:41:47Z

## Description

First-class support for untracked per-worktree context that never shows in git status. A documented scratch/context inject mode that targets a known gitignored path (configurable, default .tws/ or dev/). Auto-verify the target is gitignored. Currently users rely on manual .gitignore + hand-symlinks.
