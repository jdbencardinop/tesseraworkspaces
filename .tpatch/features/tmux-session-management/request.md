# Feature Request: Add tws close command to kill tmux sessions for a worktree. Detect if a worktree has an associated tmux session (via session naming convention). Prevent conflicts between tmux mode and direct mode when a stale tmux session exists. Show tmux session status in tws list.

**Slug**: `tmux-session-management`
**Created**: 2026-05-05T06:49:11Z

## Description

Add tws close command to kill tmux sessions for a worktree. Detect if a worktree has an associated tmux session (via session naming convention). Prevent conflicts between tmux mode and direct mode when a stale tmux session exists. Show tmux session status in tws list.
