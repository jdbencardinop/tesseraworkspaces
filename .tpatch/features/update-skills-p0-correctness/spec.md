# Specification: update-skills-p0-correctness

## Acceptance Criteria

1. Claude and Copilot skills document omitted vs explicit base semantics.
2. Skills document feature-directory inference and ambiguous multi-repo behavior.
3. Skills state that `sync --continue` resumes descendants and verifies ancestry before completion.
4. `make all` and golangci-lint pass.
