# Analysis: update-skills-p0-correctness

The embedded skills still describe pre-P0 base and continuation behavior. Agents need literal base-ref semantics, repo-scoped default detection, feature-directory inference, and guaranteed descendant continuation to avoid repeating old workarounds.
