# Feature Request: Add a shortcut to tws add that creates a feature, creates a first worktree branch, and optionally opens it in one command. Something like tws add <feature> -n <branch> [--base <parent>] [--open]. Saves running tws add + tws new + tws open separately for the common case of starting fresh work.

**Slug**: `quick-start-add-and-open`
**Created**: 2026-05-19T19:24:39Z

## Description

Add a shortcut to tws add that creates a feature, creates a first worktree branch, and optionally opens it in one command. Something like tws add <feature> -n <branch> [--base <parent>] [--open]. Saves running tws add + tws new + tws open separately for the common case of starting fresh work.
