# Analysis

Checkout mode now has logical branches, transactional sync, and exclusive agent sessions, but doctor still assumes linked worktrees and cannot explain current branch ownership, stale stack ancestry, locks, or recovery state. Observability must be read-only and mode-specific while external output remains compatible.
