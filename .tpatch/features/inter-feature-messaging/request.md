# Feature Request: Allow orchestrators in separate features under the same workspace to exchange durable targeted messages without merging feature workspaces. Extend the decisions model with a feature target (`--to-feature`), workspace-level storage/routing, per-feature read tracking, and clear source/target identities. Keep each feature's stack, inject files, and lifecycle independent. Update embedded skills when agent-facing behavior changes.

**Slug**: `inter-feature-messaging`
**Created**: 2026-07-22T05:45:39Z

## Description

Allow orchestrators in separate features under the same workspace to exchange durable targeted messages without merging feature workspaces. Extend the decisions model with a feature target (`--to-feature`), workspace-level storage/routing, per-feature read tracking, and clear source/target identities. Keep each feature's stack, inject files, and lifecycle independent. Update embedded skills when agent-facing behavior changes.
