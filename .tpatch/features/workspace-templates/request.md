# Feature Request: Add configurable templates for new features. When tws add creates a feature, use a template directory (~/.config/tws/templates/ or .tws/templates/) to populate the inject/ folder with default files like CLAUDE.local.md, skills, etc. Users can customize what every new feature gets out of the box.

**Slug**: `workspace-templates`
**Created**: 2026-05-12T03:04:11Z

## Description

Add configurable templates for new features. When tws add creates a feature, use a template directory (~/.config/tws/templates/ or .tws/templates/) to populate the inject/ folder with default files like CLAUDE.local.md, skills, etc. Users can customize what every new feature gets out of the box.
