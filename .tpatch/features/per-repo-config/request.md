# Feature Request: Add support for a .tws/config.yaml in the repo root for per-repo configuration. This would override global config for that repo. Settings like agent_command, use_tmux, and workspace path could be set per-repo and committed. Deferred from user-defined-workdir.

**Slug**: `per-repo-config`
**Created**: 2026-05-04T06:23:38Z

## Description

Add support for a .tws/config.yaml in the repo root for per-repo configuration. This would override global config for that repo. Settings like agent_command, use_tmux, and workspace path could be set per-repo and committed. Deferred from user-defined-workdir.
